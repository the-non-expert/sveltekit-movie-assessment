<script lang="ts">
  interface Props {
    title?: string;
    description?: string;
    image?: string;
    url?: string;
    type?: 'website' | 'article';
    keywords?: string;
  }

  let {
    title = 'FilmShelf - Discover and Track Your Favorite Movies',
    description = 'Explore thousands of movies, create your personal watchlist, and never miss a great film. FilmShelf is your ultimate movie discovery platform.',
    image = 'https://filmshelf-ayush.netlify.app/og-image.jpg',
    url = 'https://filmshelf-ayush.netlify.app',
    type = 'website',
    keywords = 'movies, films, movie database, watchlist, movie reviews, cinema, entertainment, IMDB'
  }: Props = $props();

  const siteName = 'FilmShelf';
  const twitterHandle = '@filmshelf';
</script>

<svelte:head>
  <!-- Primary Meta Tags -->
  <title>{title}</title>
  <meta name="title" content={title} />
  <meta name="description" content={description} />
  <meta name="keywords" content={keywords} />

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content={type} />
  <meta property="og:url" content={url} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={image} />
  <meta property="og:site_name" content={siteName} />
  <meta property="og:locale" content="en_US" />

  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image" />
  <meta property="twitter:url" content={url} />
  <meta property="twitter:title" content={title} />
  <meta property="twitter:description" content={description} />
  <meta property="twitter:image" content={image} />
  <meta property="twitter:site" content={twitterHandle} />
  <meta property="twitter:creator" content={twitterHandle} />

  <!-- Additional Meta Tags -->
  <meta name="robots" content="index, follow" />
  <meta name="language" content="English" />
  <meta name="author" content="FilmShelf" />

  <!-- Canonical URL -->
  <link rel="canonical" href={url} />
</svelte:head>
